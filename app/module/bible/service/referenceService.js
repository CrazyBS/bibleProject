define(['angular','lodash'], function (angular,_) {
  var module = angular.module('bibleApp.referenceService', []);

  module.service('VersionModel', ['$http', function ($http) {
    var service = this;


  }]);

});
